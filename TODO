To migrate to V2:

cp -rf /var/www/wx/images/* /srv/images/
cp -rf /var/www/wx/audio/* /srv/audio/noaa/
cp -rf /var/www/wx/meteor/audio/* /srv/audio/meteor/
cp $HOME/raspberry-noaa/panel.db $HOME/raspberry-noaa/db/

Once migrated, you can destroy old resources:

rm -rf $HOME/raspberry-noaa
sudo rm -rf /var/www/wx
rm $HOME/.noaa.conf

---
TODO:
* Update webpanel links for images to new image paths (above).
* Update scripts for audio and image directory adjustments (above) in scripts/.
* Create way to handle future database migrations.
* Update NOAA_OUTPUT and METEOR_OUTPUT in scripts for new location.
